<?xml version="1.0" encoding="utf-8"?>
<unattend xmlns="urn:schemas-microsoft-com:unattend" 
          xmlns:wcm="http://schemas.microsoft.com/WMIConfig/2002/State">
  <!--
    GWIG Autounattend Master Configuration
    
    This is a template that gets processed by Build-Autounattend.ps1 to generate
    the final autounattend.xml file. The build system:
    
    1. Loads a profile (e.g., gaming-gameros.json)
    2. Processes each pass file from Passes/ directory
    3. Applies variable substitutions from the profile
    4. Assembles the final autounattend.xml
    
    DO NOT EDIT THIS FILE DIRECTLY FOR DEPLOYMENT CHANGES.
    Instead, edit the profile in Profiles/ or the pass files in Passes/
  -->
  
  <settings pass="offlineServicing">
    <!-- Windows updates and driver injection during offline servicing -->
  </settings>
  
  <settings pass="windowsPE">
    <!-- Disk configuration, product key, WinPE settings -->
  </settings>
  
  <settings pass="generalize">
    <!-- Sysprep generalization settings -->
  </settings>
  
  <settings pass="specialize">
    <!-- Device-specific configuration, RunSynchronous commands -->
  </settings>
  
  <settings pass="auditSystem">
    <!-- Audit mode system settings -->
  </settings>
  
  <settings pass="auditUser">
    <!-- Audit mode user settings -->
  </settings>
  
  <settings pass="oobeSystem">
    <!-- OOBE configuration and FirstLogonCommands -->
  </settings>
  
  <!-- Extensions section for embedded scripts (optional) -->
  <Extensions xmlns="urn:winhance:unattend">
    <ExtractScript>
param([xml] $Document);
$scriptsDir = 'C:\ProgramData\Winhance\Unattend\Scripts\';
foreach( $file in $Document.unattend.Extensions.File ) {
    $path = [System.Environment]::ExpandEnvironmentVariables($file.GetAttribute('path'));
    if( $path.StartsWith($scriptsDir) ) { mkdir -Path $scriptsDir -ErrorAction 'SilentlyContinue'; }
    $encoding = switch([System.IO.Path]::GetExtension($path)) {
        { $_ -in '.ps1', '.xml' } { [System.Text.Encoding]::UTF8; }
        { $_ -in '.reg', '.vbs', '.js' } { [System.Text.UnicodeEncoding]::new($false, $true); }
        default { [System.Text.Encoding]::Default; }
    };
    [System.IO.File]::WriteAllBytes($path, ($encoding.GetPreamble() + $encoding.GetBytes($file.InnerText.Trim())));
}
    </ExtractScript>
  </Extensions>
</unattend>
